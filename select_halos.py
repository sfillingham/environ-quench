"""

"""

import numpy as np
import pandas as pd
from astropy.table import Table

def get_centrals(halocat, mass_range=[1.e12, 1.e15]):
    """This function will select all halos in the simulation
    based on the mass range specified.

    Parameters
    ----------
    halocat : pd.DataFrame
        pandas DataFrame containing the following columns
    mass_range : tuple
        min_mass, max_mass

    Returns
    -------
    centrals : pd.DataFrame

    """

    return centrals
